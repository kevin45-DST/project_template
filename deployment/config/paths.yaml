# Syntaxe : ${NOM_VARIABLE:-valeur_par_defaut}
project:
  root_folder: "{{PROJECT_ROOT_FOLDER}}"

data:
  root_folder: data 
  raw: raw
  processed: processed

persistence:
  root_folder: models

reports:
  root_folder: reports
  search: search
  training: training

tracking:
  root_folder: tracking
  mlflow:
    root_folder: mlflow
    artifacts: artifacts

logs:
  root_folder: logs